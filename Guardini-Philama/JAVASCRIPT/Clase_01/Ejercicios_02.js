//Ejercicio 2: Crear un rango de numeros de 2 a 6 e imprimelos
//Ejempl de ejecucion: 2, 3, 4, 5, 6

// Usamos un array para representar el "rango"
const rango = [2, 3, 4, 5, 6];

// Recorremos cada número en el array "rango"
for (let i = 0; i < rango.length; i++) {
  // Imprimimos el valor actual del número en la consola
  console.log(rango[i]);
}